import React from 'react';
import styled from 'styled-components';

const H1 = styled.h1 `
    font-size: 55px;
`

const Nav = (props) => {
    return(
        <H1>Thundercats Memory Game</H1>
    )
}

export default Nav;